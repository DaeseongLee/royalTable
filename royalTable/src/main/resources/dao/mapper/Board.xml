<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.mapper.BoardMapper">
	<select id="count" parameterType="map" resultType="int">
		select count(*) from board
		<if test="searchtype != null and searchcontent != null">
			where ${searchtype} like '%${searchcontent}%'
			<if test="col2 != null">
				or ${col2} like '%${searchcontent}%'
			</if>
		</if>
	</select>
	<select id="list" parameterType="map" resultType="Board">
		select boardNo, userId, subject, content, type, reflevel, cnt, regDate from board 
		<if test="boardNo != null">
			where boardNo = #{boardNo}
		</if>
		<if test="searchtype != null and searchcontent != null">
			where ${searchtype} like '%${searchcontent}%'
			<if test="col2 != null">
			or ${col2} like '%${searchcontent}%'
		</if>
		</if>
		order by boardNo desc, reflevel asc limit #{startrow}, #{limit}
	</select>
	<select id="list2" parameterType="map" resultType="Board">
		select boardNo, userId, subject, content, type, reflevel, cnt, regDate from board 
		<if test="boardNo != null">
			where boardNo = #{boardNo}
		</if>
	</select>
</mapper>